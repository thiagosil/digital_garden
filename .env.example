# Database URL
# For local development with Turso Dev (run: turso dev):
DATABASE_URL="http://127.0.0.1:8080"

# For production with Turso (recommended for Vercel):
# DATABASE_URL="libsql://[your-database].turso.io"
# TURSO_AUTH_TOKEN="your-auth-token"

# Note: TURSO_AUTH_TOKEN is only required when using Turso remote/cloud database
# Leave it empty or unset for local development with 'turso dev'

# Authentication
# JWT secret key for signing session tokens (required for production)
# Generate a random string: openssl rand -base64 32
JWT_SECRET="your-secret-key-change-this-in-production"

# External API Keys
# TMDB API (The Movie Database) - for movies and TV shows search
# Use the API Read Access Token (Bearer token) for secure authentication
# Get your token from: https://www.themoviedb.org/settings/api
TMDB_ACCESS_TOKEN="your-tmdb-read-access-token"

# RAWG API - for video games search
# Get your API key from: https://rawg.io/apidocs
RAWG_API_KEY="your-rawg-api-key"
